const cBcBody = document.querySelector('.cBcBody');
for (let i = 0; i < 3; i += 1) {
    const button = document.createElement('button');
    button.classList.add('buttons');
    cBcBody.appendChild(button);
}

const cBcSectionUm = document.querySelector('.cBcSectionUm');
for (let i = 0; i < 3; i += 1) {
    const button = document.createElement('button');
    button.classList.add('buttons');
    cBcSectionUm.appendChild(button);
}
